# ========================================
# Logo Designer - Environment Variables
# ========================================

# This file shows all environment variables used across the monorepo.
# Copy relevant sections to:
# - `.env.local` for Docker Compose development
# - Coolify app settings for production deployment

# ========================================
# DATABASE (Postgres)
# ========================================
DB_USER=logouser
DB_PASSWORD=logopass
DB_NAME=logodb
DB_PORT=5432

# Connection string format for API:
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@host:${DB_PORT}/${DB_NAME}

# ========================================
# REDIS (Future use)
# ========================================
# REDIS_PORT=6379
# REDIS_URL=redis://redis:6379

# ========================================
# API SERVICE
# ========================================
# Server configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# CORS - Set to your web app URL
CORS_ORIGIN=http://localhost:3000

# Database connection (production)
# DATABASE_URL=postgresql://user:pass@db-host:5432/logodb

# ========================================
# WEB SERVICE
# ========================================
# API endpoint URL
VITE_API_URL=http://localhost:3001

# ========================================
# DEPLOYMENT NOTES
# ========================================
#
# LOCAL DEVELOPMENT (docker-compose.dev.yml):
# - Copy this to .env.local
# - Use service names as hosts (db, redis, api)
# - Example: DATABASE_URL=postgresql://logouser:logopass@db:5432/logodb
#
# COOLIFY PRODUCTION:
# - Set env vars in Coolify UI for each app separately
# - Web app: Set VITE_API_URL=https://fcc-business-api.inherent.design
# - API app: Set CORS_ORIGIN=https://fcc-business.inherent.design
# - API app: Set DATABASE_URL to your managed Postgres instance
# - Static ports: Web=3000, API=3001 (configured in Coolify)
